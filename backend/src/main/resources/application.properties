spring.application.name=WishportAPI


#Puerto donde se encender· el servidor (el camarero)
server.address=0.0.0.0
server.port=8080

#ConexiÛn a la base de datos MySQL local
spring.datasource.url=jdbc:mysql://localhost:3306/wishport_db
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JPA / Hibernate (Para que Spring entienda SQL)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# ConfiguraciÛn Jackson para fechas ISO UTC
spring.jackson.time-zone=UTC


# Servir archivos est√°ticos (ya viene por defecto, pero expl√≠cito)
spring.web.resources.static-locations=classpath:/static/

# Tama√±o m√°ximo de archivos subidos (si usas upload)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
